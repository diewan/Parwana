[package]
name = "csv-admission"
version = "0.1.5"
edition = "2024"
rust-version = "1.95"

[dependencies]
csv-protocol = { path = "../csv-protocol" }
thiserror = "1.0"
tracing = "0.1"

[features]
default = []
