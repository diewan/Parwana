# cargo-deny configuration.
#
# Cargo-deny uses this file for graph selection. It does not support arbitrary
# forbidden dependency edges. CSV's architectural edges are therefore enforced
# by csv-architecture/tests/dep_graph_constitution.rs and architecture_guard.rs,
# both of which run in CI.

[graph]
targets = []
all-features = true
