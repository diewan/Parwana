chain_id = "ethereum-sepolia"
chain_name = "ethereum-sepolia"
default_network = "testnet"
rpc_endpoints = ["https://rpc.sepolia.org"]
block_explorer_urls = ["https://sepolia.etherscan.io"]
start_block = 0

[finality_guarantee]
max_reorg_depth = 12
is_probabilistic = false
validator_honesty_threshold = 0.67
proof_system = { type = "EthereumPos", finality_epochs = 2 }
# Upper bound on tip - anchor for accepted lock proofs (VERIFY-PROOF-FRESHNESS-001).
# Aligned with the CSVSeal escrow refund window (24h ≈ 7200 Sepolia blocks): a lock
# older than the refund timeout is genuinely stale; anything younger must remain
# resumable (finality wait + operator resume routinely exceeds 100 blocks).
max_proof_age_blocks = 7200
min_anchor_sources = 1

[capabilities]
state_model = "Account"
finality_model = "FinalizedCheckpoint"
finality_depth = 2
deterministic_finality = true
proof_model = "MerklePatricia"
replay_protection = "SmartContractNullifier"
native_single_use_semantics = false
reorg_risk = "Medium"
max_safe_reorg_depth = 12
supports_light_client_proofs = true
supports_state_proofs = true
supports_transaction_inclusion_proofs = true
supports_offline_verification = true
supports_zk_proofs = true
chain_role = "Settlement"

[custom_settings]
# Contract address where CSVSeal is deployed
contract_address = "0xba3838a7105b0131d418ac344bf999f420b12938"
